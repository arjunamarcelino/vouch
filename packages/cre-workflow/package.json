{
  "name": "@vouch/cre-workflow",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Chainlink CRE Confidential Workflow — runs the private regression verdict inside the enclave (handlerInTee + ConfidentialHTTPClient) and writes a DON-signed 7-tuple report to AssuranceHub on Arc.",
  "//zod": "Pinned to 3.25.76 (NOT the workspace catalog v4): @chainlink/cre-sdk@1.20.1 depends on zod v3, and Runner.newRunner requires the config schema to be built with the SAME zod instance (plan §16, §17.11-7). Do not switch to catalog: here. Do NOT import @vouch/shared into this package either — it pins zod via catalog (v4) and would reintroduce the two-copies mismatch this pin guards against; keep any reused taxonomy (e.g. RefuseReason) local.",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test '*.test.ts'",
    "simulate:harness": "node --import tsx harness.ts",
    "simulate": "echo 'RUNNABLE NOW (deterministic local proof, no CRE account/secrets): pnpm --filter @vouch/cre-workflow simulate:harness fixtures/<fixture>.json  (e.g. fixtures/valid-failure.json).  SUBMISSION EVIDENCE (needs the CRE CLI + a CRE account): cre workflow simulate <name> --target staging-settings --non-interactive --trigger-index 0 --evm-tx-hash <hash> --evm-event-index <n>  — this is an EVM-LOG trigger (ClaimOpened), so use --evm-tx-hash/--evm-event-index, NOT --http-payload. See README/docs.'"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "1.20.1",
    "viem": "catalog:",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@vouch/config": "workspace:*",
    "tsx": "^4.19.2"
  }
}
