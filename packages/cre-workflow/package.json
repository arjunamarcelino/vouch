{
  "name": "@vouch/cre-workflow",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Chainlink CRE Confidential Workflow — runs the private regression verdict inside the enclave (handlerInTee + ConfidentialHTTPClient) and writes a DON-signed 7-tuple report to AssuranceHub on Arc.",
  "//zod": "Pinned to 3.25.76 (NOT the workspace catalog v4): @chainlink/cre-sdk@1.20.1 depends on zod v3, and Runner.newRunner requires the config schema to be built with the SAME zod instance (plan §16, §17.11-7). Do not switch to catalog: here.",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test '*.test.ts'",
    "simulate": "echo 'Run: cre workflow simulate <name> --target staging-settings --non-interactive --trigger-index 0 --http-payload ./fixtures/<fixture>.json  (install the CRE CLI first — see README/docs)'"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "1.20.1",
    "viem": "catalog:",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@vouch/config": "workspace:*",
    "tsx": "^4.19.2"
  }
}
