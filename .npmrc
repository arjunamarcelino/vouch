# pnpm configuration
# Keep a strict, non-hoisted store; workspace packages resolve via workspace:* links.
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
# Let engines mismatches warn instead of hard-failing (dev env may run newer Node).
engine-strict=false
